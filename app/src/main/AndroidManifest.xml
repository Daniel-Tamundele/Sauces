<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.modile.sauces">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Sauces"
        tools:targetApi="31">
        <activity
            android:parentActivityName=".ProfilActivity"
            android:name=".Modifier_ProfilActivity"
            android:exported="false" />
        <activity
            android:name=".ListDocActivity"
            android:exported="false"
            android:label="ListDoc"
            android:parentActivityName=".HomePageActivity" />
        <activity
            android:name=".ProfilActivity"
            android:exported="false"
            android:label="Profil"
            android:parentActivityName=".HomePageActivity" />
        <activity
            android:name=".AjoutDocActivity"
            android:exported="false"
            android:label="AjoutDoc"
            android:parentActivityName=".HomePageActivity" />
        <activity
            android:name=".HomePageActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LoginActivity"
            android:exported="false"
            android:label="Login" />
    </application>

</manifest>